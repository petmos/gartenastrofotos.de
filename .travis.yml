language: go
sudo: required
install:
  - go get -v github.com/spf13/hugo
  - sudo pip install --upgrade pip
  - sudo pip install pygments
  - sudo pip install s3cmd
script:
  - hugo
  - s3cmd sync --delete-removed -P -M -r public/ s3://${AWS_BUCKET_NAME}/